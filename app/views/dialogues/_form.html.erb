<center>
<%= form_for(@dialogue) do |f| %>
  <% if @dialogue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dialogue.errors.count, "error") %> prohibited this dialogue from being saved:</h2>

      <ul>
      <% @dialogue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions" value="JOIN DEBATE CLUB!">
    <%= f.submit %>
  </div>
<% end %>
</center>
